//1. Let,const : thay thế var
    //Var / Let,Const : Scope,hosting (phạm vi truy cập)
    //Const / Var,Let : Assignment(Const không gán lại được, chỉ gán được trong thuộc tính của nó)
    // => Dùng Const: Định nghĩa biến và không gán lại biến đó
    // => Dùng Let: Định nghĩa biến và cần gán lại biến đó

//2. Template Literals  
// 3. Multi-line String

// 4. Arrow function
// 5. Classes
    class courses{
        constructor(name,price){
            this.nam = name;
            this.price = price;
        }
        getName(){
            return this.name;
        }
        getPrice(){
            return this.price;
        }
    }
    const php = new courses('PHP',1000);
    
// 6. Default parameter values
// 7. Destructuring
// 8. Rest parameters
// 9. Spread
// 10. Enhanced object literals
// 11. Tagged template literal
// 12. Modules

